# Unc Consumer

AWS Lambda function that consumes messages from an SQS queue and processes them based on message type.

### Deployment
Deploy using your preferred CI/CD pipeline or AWS SAM/CloudFormation templates.